import { LoadingScreen } from "@/components/LoadingScreen";

// The house "27" transition while this screen's server data resolves — the same
// beat Outfits already uses, applied across the nav for a consistent feel.
export default function Loading() {
  return <LoadingScreen />;
}
