// The in-app wordmark, shown at the top of every authed screen. Heavy and
// present — smaller than the login hero, but no longer a thin afterthought.
export function Wordmark() {
  return (
    <span className="text-xl font-black uppercase tracking-tight leading-none">
      BLOCK27
    </span>
  );
}
